This is a Maven project


To download source:

git clone git://github.com/m4r35n357/JavaEe6WebApp.git


To build & test:

$ cd JavaEe6WebApp
$ mvn clean install


To run:

$ mvn embedded-glassfish:run

then browse to:

http://localhost:9090/jee6webapp

